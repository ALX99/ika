ika:
  gracefulShutdownTimeout: 1s

servers:
  - addr: :8888

namespaces:
  /example:
    plugins:
      - name: tracer
    reqModifiers:
      - name: basic-modifier
        config:
          host: http://httpbun

    middlewares:
      - name: accessLog
    paths:
      /:
        middlewares:
          - name: noCache

  httpbun.dozy.dev:
    transport:
      maxIdleConnsPerHost: 250
    plugins:
      - name: tracer
    reqModifiers:
      - name: basic-modifier
        config:
          host: https://httpbun.com
    middlewares:
      - name: accessLog
    paths:
      /: {}
      /trigger-rewrite/{anything...}:
        reqModifiers:
          - name: basic-modifier
            config:
              path: /{anything...}

  go.dozy.dev:
    plugins:
      - name: tracer
    reqModifiers:
      - name: basic-modifier
        config:
          host: https://go.dev
    middlewares:
      - name: accessLog
    paths:
      /: {}
